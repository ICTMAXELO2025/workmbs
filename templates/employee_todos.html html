{% extends "base.html" %}

{% block content %}
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">My To-Do List</h1>
    <div class="btn-toolbar mb-2 mb-md-0">
        <a href="{{ url_for('employee_todos_add') }}" class="btn btn-primary">
            <i class="fas fa-plus me-2"></i>Add Task
        </a>
    </div>
</div>

<div class="dashboard-widget">
    {% if todos %}
    <div class="row">
        <div class="col-md-6">
            <h5 class="mb-3">Pending Tasks</h5>
            {% for todo in todos if not todo.is_completed %}
            <div class="list-group-item todo-item {{ todo.priority }}-priority mb-2">
                <div class="d-flex justify-content-between align-items-center">
                    <div class="form-check">
                        <form action="{{ url_for('employee_todo_update', todo_id=todo.id) }}" method="POST" class="d-inline">
                            <input class="form-check-input" type="checkbox" onchange="this.form.submit()" {% if todo.is_completed %}checked{% endif %}>
                            <label class="form-check-label {% if todo.is_completed %}text-decoration-line-through{% endif %}">
                                {{ todo.content }}
                            </label>
                        </form>
                    </div>
                    <div>
                        {% if todo.due_date %}
                        <small class="text-muted me-2">
                            Due: {{ todo.due_date }}
                            {% if todo.due_date == today %}<span class="badge bg-warning">Today</span>{% endif %}
                        </small>
                        {% endif %}
                        <span class="badge bg-{{ 'danger' if todo.priority == 'high' else 'warning' if todo.priority == 'medium' else 'info' }}">
                            {{ todo.priority|title }}
                        </span>
                        <a href="{{ url_for('employee_todo_delete', todo_id=todo.id) }}" class="btn btn-sm btn-outline-danger ms-2" onclick="return confirm('Are you sure you want to delete this task?')">
                            <i class="fas fa-trash"></i>
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>

        <div class="col-md-6">
            <h5 class="mb-3">Completed Tasks</h5>
            {% for todo in todos if todo.is_completed %}
            <div class="list-group-item todo-item completed mb-2">
                <div class="d-flex justify-content-between align-items-center">
                    <div class="form-check">
                        <form action="{{ url_for('employee_todo_update', todo_id=todo.id) }}" method="POST" class="d-inline">
                            <input class="form-check-input" type="checkbox" onchange="this.form.submit()" {% if todo.is_completed %}checked{% endif %}>
                            <label class="form-check-label text-decoration-line-through text-muted">
                                {{ todo.content }}
                            </label>
                        </form>
                    </div>
                    <div>
                        {% if todo.due_date %}
                        <small class="text-muted me-2">Due: {{ todo.due_date }}</small>
                        {% endif %}
                        <a href="{{ url_for('employee_todo_delete', todo_id=todo.id) }}" class="btn btn-sm btn-outline-danger" onclick="return confirm('Are you sure you want to delete this task?')">
                            <i class="fas fa-trash"></i>
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
    {% else %}
    <div class="text-center py-5">
        <i class="fas fa-tasks fa-3x text-muted mb-3"></i>
        <h4>No Tasks Yet</h4>
        <p class="text-muted">You don't have any tasks in your to-do list.</p>
        <a href="{{ url_for('employee_todos_add') }}" class="btn btn-primary">Add Your First Task</a>
    </div>
    {% endif %}
</div>
{% endblock %}